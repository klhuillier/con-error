{
  "name": "con-error",
  "version": "0.0.1",
  "scripts": {
    "build-mkdist": "mkdir -p dist",
    "build-statics": "cp LICENSE.md README.md dist",
    "build-bundle": "./node_modules/.bin/browserify -d src/index -o dist/index.js -s ConError",
    "build-minify": "./node_modules/.bin/uglifyjs -cm reserved='[ConError]' dist/index.js -o dist/index.min.js",
    "build-minify-warn": "./node_modules/.bin/uglifyjs -cm reserved='[ConError]' --verbose --warn dist/index.js -o dist/index.min.js",
    "build": "npm run build-mkdist && npm run build-statics && npm run build-bundle && npm run build-minify"
  },
  "main": "./dist/index.min.js",
  "dependencies": {
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.0",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0",
    "jest": "~20.0.4",
    "uglify-js": "^3.0.27"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            [
              "env",
              {
                "targets": {
                  "browsers": [
                    "chrome >= 60",
                    "safari >= 9",
                    "ie >= 10"
                  ]
                }
              }
            ]
          ]
        }
      ]
    ]
  }
}
